apiVersion: backupcredential.taikun.jet.crossplane.io/v1alpha1
kind: Credential
metadata:
  name: backup-credential-alexis
spec:
  forProvider:
    name: "backup-credential-alexis"
    s3AccessKeyId:
    s3Endpoint:
    s3Region:
    s3SecretAccessKey:
  providerConfigRef:
    name: default
