apiVersion: showbackcredential.taikun.jet.crossplane.io/v1alpha1
kind: Credential
metadata:
  name: showback-cred-crossplane
spec:
  forProvider:
    name: "success-showcred"
    passwordSecretRef:
      key: "password"
      name: "showback-credentials"
      namespace: "crossplane-system"
    url:
    username:
    organizationId: "15687"
    lock: false
  providerConfigRef:
    name: default
