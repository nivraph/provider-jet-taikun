apiVersion: organization.taikun.jet.crossplane.io/v1alpha1
kind: AccessProfile
metadata:
  name: ap-raph-crossplane
spec:
  forProvider:
    organizationId: "15680"
  providerConfigRef:
    name: default
